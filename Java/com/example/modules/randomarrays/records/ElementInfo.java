package com.example.modules.randomarrays.records;

/**
 * Corresponds to the C++ struct ElementInfo.
 * Used to store a value and its index in an array.
 */
public record ElementInfo(int value, int index) {}
